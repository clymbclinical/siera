# Generate library loading code

Internal helper that returns the boilerplate code used to load the
packages required by generated ARD scripts.

## Usage

``` r
.generate_library_code()
```

## Value

Character string containing the library load block.
