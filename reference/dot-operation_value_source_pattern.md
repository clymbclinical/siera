# Regular expression matching the operation-ID valueSource family

\`operation_1\` ... \`operation_N\` are derived from a method's own
operations and are valid valueSources even though they are not listed by
\[.supported_value_sources()\].

## Usage

``` r
.operation_value_source_pattern()
```

## Value

A single regular expression string.
